import {dummyTestimonials} from "../DummyData/testimonials";

const initialState = dummyTestimonials;

const testimonialsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            break;
    }

    return state;
};

export default testimonialsReducer
